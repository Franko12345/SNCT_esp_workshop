from robot import *
import time
from machine import Pin, PWM

while (True):
    dst = medir_distancia_cm()
    print(dst)
    if(dst > 10):
        andar_frente()
    else:
        parar()
    time.sleep(0.1)
